---
title: TODO
description: 当前项目后续值得处理的事项
---

# TODO

本文档用来记录当前项目后续比较值得处理的事项。

- CPA 升级到 ≥v7.2.118，解锁 `grok-imagine-video-1.5` 正式名与 Image 2.0（画布目前仍走 preview 别名）。
- 组装提示词开启但用户没 `@` 引用上游节点时，上游内容是自动并入还是明确提示，语义待定。现在上游正文会被丢掉，角标却仍显示已连接。
- 连上上游图时自动切到改图模型（`grok-imagine-edit` / GPT 改图路径）。
- 切到生图标签后，下拉里不应残留视频模型。
- 新建生成配置节点不要叠在当前选中节点的坐标上。
- GPT 透明背景待实测；开关先保留，确认上游是否真支持后再定。
- 本地 Agent 接入后续：按[本地 Agent 接入规划](/zh-CN/docs/progress/local-agent-integration-plan)把 Claude Code CLI Adapter 升级为 Claude Agent SDK Adapter，并完善工具队列体验。
- Agent Skill 后续：补充网络检索安装、Skill 资源文件管理和可控的本地记忆沉淀。
