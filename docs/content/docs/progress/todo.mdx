---
title: TODO
description: Planned follow-up work
---

# TODO

- Upgrade CPA to ≥v7.2.118 so the official `grok-imagine-video-1.5` name and Image 2.0 unlock (the canvas currently maps 1.5 through the preview alias).
- Decide what happens when compose-prompt is on but the user never `@`-mentions an upstream node: merge the upstream content automatically, or show a clear prompt. Today the upstream text is dropped while the badge still looks connected.
- When an upstream image is connected, automatically switch the generation model to the edit model (`grok-imagine-edit` / GPT edit path).
- Clear leftover video models from the image-tab model picker after switching capabilities.
- New generation-config nodes should not stack on the currently selected node’s coordinates.
- Measure whether GPT Image 2 actually honors transparent background; keep the switch until that is confirmed.
- Replace the Claude Code CLI adapter with a Claude Agent SDK adapter and improve the tool queue, following the [Local Agent Integration Plan](/docs/progress/local-agent-integration-plan).
- Add Skill installation from web search, Skill resource-file management, and controlled local-memory persistence.
